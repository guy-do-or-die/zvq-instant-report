define('app/views/MenuView',
    [
        'ember',
        'app/config/Application'
    ],
    function(Em, App) {
        'use strict';

        App.MenuView = Em.View.extend({
        });

        return App.MenuView;
    });
